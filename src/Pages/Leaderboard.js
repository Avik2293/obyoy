import React from 'react';
import { ButtonGroup, Container, IconButton, HStack, Stack, Text, Box, SimpleGrid, GridItem, Grid, Center, Divider, Link as ChakraLink, LinkProps, textDecoration } from '@chakra-ui/react'
// import { Link } from 'react-router-dom';
// import { Link as ReactRouterLink } from 'react-router-dom';


const Leaderboard = () => {

    return (
        <Container pb={{ base: '2', md: '4', }} px={{ base: '3', md: '8', }} maxWidth={"1400px"} mx={'auto'} >

            Leaderboard

        </Container>
    );
};

export default Leaderboard;